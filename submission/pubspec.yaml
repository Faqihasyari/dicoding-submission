name: online_image_classification
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  image_picker: ^1.1.2
  image_cropper: ^12.1.1
  camera: ^0.10.6
  tflite_flutter: ^0.12.1
  firebase_core: ^4.6.0
  firebase_ml_model_downloader: ^0.4.1
  image: ^4.1.3
  http: ^1.6.0
  google_generative_ai: ^0.4.7
  envied: ^1.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  envied_generator: ^1.3.4
  build_runner: ^2.13.1

flutter:
  uses-material-design: true
