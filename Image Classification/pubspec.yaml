name: online_image_classification
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  camera: ^0.10.6
  flutter:
    sdk: flutter
  http: ^1.6.0
  image: ^4.8.0
  image_picker: ^1.1.2
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
